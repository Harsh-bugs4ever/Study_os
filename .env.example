# Frontend transport configuration. The names remain unchanged so the existing
# frontend needs no source edits; the URL points to the self-hosted backend.
VITE_SUPABASE_URL=http://localhost:8000
VITE_SUPABASE_PUBLISHABLE_KEY=local-anon-key

# Optional frontend OAuth client identifier retained for current integrations.
VITE_GITHUB_CLIENT_ID=
